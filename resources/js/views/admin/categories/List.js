import React from 'react';

import MasterLayout from '../layouts/Master';

const List = props => (
    <MasterLayout {...props}>
        <h1>List of Categories</h1>
    </MasterLayout>
);

export default List;
